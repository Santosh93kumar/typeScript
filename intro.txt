typescript is Superset of js
it gives type safety


npm init -y   ===> node project intilliation
npm i -D typescript ==\> typeScript insatll in devDepency
npx tsc --init   ==> typeScript project initiallation



type inferencing 
where the types is automatically decleared
type annotation
where we need to delare the type

there are two type of error
1. syntax error 2 type error


unkonown is different fron any in call construct and access property


Use interface when:
Working with objects
Building APIs / classes
Need extensibility / merging

👉 Example: React props, backend models

✅ Use type when:
Need unions (|)
Need tuples
Need utility types
Complex compositions


//ops is ts is missing



"Functions with void return type will always return undefined, so logging their result prints undefined.

Feature	        ()Small Brackets	    {} Big Brackets
Purpose	        Function	            Object
Used for        Function type	        Object shape
Example	        (x:number)=>number      {name:string}
Inside interface Call signature	        Properties / index signature



to install some library 
npm i -D @types/lib-name 
-D => common for dev depency
@types => for it's types